'use client'

import { useState, useEffect} from "react"

export default function GemLocator() {
    
const [gems, setGems] = useState()

// maybe I just want to have a search bar here and we can look for cities in the db? or someting like this
}